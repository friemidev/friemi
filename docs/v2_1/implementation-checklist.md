# v2.1 功能实现清单

## 当前阶段目标

v2.1 的重点是在 v2.0 完成迁移、品牌基础和核心体验修复后，继续提升产品第一印象和长期维护质量。这个版本不优先堆新功能，而是把首页视觉质感、响应式布局边界和移动端 / 网页端样式架构整理清楚。

当前主线：

```text
v2.0 发布稳定 -> 首页形成更高级的品牌第一印象 -> 移动端和网页端布局彻底解耦 -> 后续 UI 迭代更可控
```

## 分支规则

所有功能从 `dev` 拉独立 feature 分支：

```bash
git switch dev
git pull friemi dev
git switch -c feature/功能名
```

完成后 PR 到：

```text
feature/* -> dev
```

v2.1 不建议把首页视觉重构和响应式架构拆分混在同一个 PR。首页重构偏体验与视觉验证；移动端 / 网页端样式分离偏架构和回归风险，应分开评审和预览。

## P0：v2.1 重点体验优化

### 1. 首页样式优化：高星级酒店式品牌质感

建议分支：

```text
feature/v2-1-home-luxury-hotel-redesign
```

需求理解：

首页需要从普通活动工具首页，升级成更有 Friemi 识别度和信任感的第一屏。视觉方向参考高星级酒店官网的克制、高级、清爽和服务感，但不能做成空洞的营销页。用户打开首页后应该马上理解：Friemi 是一个适合发现活动、约朋友、发起组局的精致生活方式工具。

小功能：

- [ ] 重构首页第一屏视觉，参考高星级酒店官网的留白、字体层级、图片质感和低饱和色彩
- [ ] 首页首屏明确展示 Friemi 品牌、活动发现、组局和朋友同行的核心价值
- [ ] 使用高质量真实感图片或品牌资产，不使用廉价渐变、装饰色块或模板感插画
- [ ] 首页 CTA 层级重排，优先突出发现活动、进入组队大厅和发起组局
- [ ] 最近活动 / 最近组局模块使用更高级的卡片密度和排版，不显得杂乱或像后台列表
- [ ] 移动端首页首屏不被大段文案占满，优先看到品牌、核心入口和内容预览
- [ ] 桌面端首页避免大面积空白，内容宽度、图片比例和入口数量更平衡
- [ ] 首页 footer、header 和主体视觉风格保持一致，不出现 v1/v2 风格混搭

验收标准：

- 首页第一屏有明显的高级感、服务感和 Friemi 品牌识别度
- 用户能在 3 秒内理解产品用途：发现活动、约朋友、一起出发
- 移动端 390px 不出现横向滚动条，首屏不被纯文案撑满
- 桌面端 1280px 下布局不空、不散、不像临时模板
- 主要 CTA 触控高度不低于 44px
- 图片、按钮、卡片和文字层级统一，不出现廉价营销页质感

### 1.1 移动端新版首页和底部导航布局调整

建议分支：

```text
feature/v2-1-mobile-home-bottom-nav-redesign
```

需求理解：

新增一个移动端专属大厅页 `/mobile-home`，按新示意图重新组织首屏和底部导航；原有 `/home` 移动端首页保持不变，header logo 继续回到原来的 `/home`。示意图只作为布局和动线参考，不做逐像素照抄。最终视觉需要继续融入 Friemi 当前 v2.1 的奶油纸面、品牌绿、珊瑚强调、圆角卡片和轻量插画风格。新增参考图位于：

```text
apps/web/public/illustrations/design.png
```

可使用素材目录：

```text
apps/web/public/illustrations/png
apps/web/public/illustrations/vector
```

小功能：

- [x] 新建 `/mobile-home` 移动端大厅页结构：顶部品牌区、主推荐 / 广告位、分类插画入口、近期内容或发现入口，避免只有大段文案
- [x] 使用 `apps/web/public/illustrations` 下的插画素材作为分类入口视觉，但颜色、圆角、阴影和排版需要融入 v2.1 品牌风格
- [x] 主推荐 / 广告位卡片可以比普通入口更大，但不能显得像临时 banner 或廉价广告框
- [x] 移动端底部导航顺序按示意图调整为：活动、组局、大厅、消息、主页
- [x] 将底部导航最中间主入口命名为“大厅”，指向移动端专属大厅页 `/mobile-home`
- [x] 底部导航中间“大厅”区块更大、更突出，其余四个入口更小但保持 44px 触控面积
- [x] 组局入口指向当前 `/lobby`，作为普通入口，不再占据底部导航最中心的主按钮位置
- [x] `/mobile-home` 接入 lobby 的滑动发现 swiper，并保留分页加载和去重逻辑
- [x] 将移动端大厅九宫格升级为活动 / 组局全局统一分类：饭局、闲逛、视听、艺术、桌游、进步、旅行、音乐、运动，并保留“其它”作为第 10 个兜底分类
- [x] 补充分类 enum 和历史数据迁移：旧电影 / 戏剧归入视听，旧展览归入艺术，旧其它保持其它，避免生产数据被误分
- [x] 移动端首页和底部导航的中 / 英 / 法标签不出现难看的省略号、文字切割或挤压
- [x] 桌面端首页和桌面导航不在本分支大改，避免移动端视觉调整反向破坏网页端

验收标准：

- 390px `/mobile-home` 首屏信息更充实，能看到主入口和插画分类，不出现横向滚动条
- 底部导航中间“大厅”视觉优先级最高，其他四个入口层级清楚
- 底部导航顺序与示意图一致，用户能快速找到活动、组局、大厅、消息和主页
- 活动、组局、搜索、后台导入和链接解析使用同一套 9+1 分类，不再出现旧分类与移动端入口不一致
- 插画素材与 Friemi v2.1 品牌风格融合，不像外部素材直接拼贴
- 底部导航不遮挡首页核心按钮、卡片内容或弹窗

### 2. 移动端 CSS 与网页端 CSS / 布局彻底分离

建议分支：

```text
feature/v2-1-responsive-style-split
```

需求理解：

当前很多页面通过同一套组件和 Tailwind breakpoint 同时处理移动端与网页端，导致移动端优化时容易破坏桌面端，桌面端美化时又会让移动端出现拥挤、错位或空间浪费。v2.1 需要逐步建立更明确的移动端 / 网页端样式边界，必要时连布局结构也分开，而不是只靠一套 DOM 加响应式 class 硬撑。

小功能：

- [ ] 盘点核心页面中移动端和网页端互相牵制最严重的组件：home、activities、lobby、search、detail、profile
- [ ] 制定移动端 / 网页端样式分离规则：哪些只分 CSS，哪些需要拆组件或拆布局容器
- [x] 为首页建立清晰的 mobile layout 和 desktop layout 边界，避免复杂 breakpoint 串联
- [x] 首页活动轮播增加 mobile / desktop density 边界入口，本分支保持原有卡片宽度、字号和视觉参数不变
- [ ] 移动端优先优化首屏密度、底部导航安全区、触控高度和双列卡片宽度
- [ ] 网页端优先优化内容密度、栅格列数、筛选栏横向布局和大屏留白
- [ ] 避免移动端隐藏元素仍影响桌面端布局，或桌面端容器约束反向影响移动端
- [ ] 梳理共享视觉 token，只共享颜色、字体、圆角、阴影等基础语义，不强行共享布局
- [x] 为后续页面重构记录可复用约定，避免每个页面重新发明响应式结构

验收标准：

- 移动端和网页端可以分别调整核心布局，不再频繁互相破坏
- 390px 移动端核心页面无横向滚动条、底部导航不遮挡主要操作
- 1280px 桌面端核心页面无明显大空白、卡片列数和筛选布局合理
- 组件代码中的 breakpoint 复杂度下降，页面结构更容易阅读和维护
- 后续 UI PR 可以清楚说明影响的是 mobile、desktop 还是共享 token

### 3. 全站品牌风格与 Logo 大整改

建议分支：

```text
feature/v2-1-brand-style-overhaul
```

需求理解：

v2.1 后续需要把 Friemi 的视觉从“局部页面优化”推进到全站统一整改。重点不是只替换一张 logo，而是同步梳理网页端和移动端 header、footer、loading、空态、按钮、卡片、背景和整体视觉调性，让用户在任何入口都感到这是同一套新品牌系统。

小功能：

- [x] 重新确认网页端和移动端 logo 使用策略：图标、文字标题、组合 logo、小尺寸 favicon / app icon
- [x] 建立共享品牌组件：`BrandLockup` 承载横向 logo 组合图，`BrandBackdrop` 承载 desktop / mobile 品牌背景资产
- [x] 分别优化网页端 header 和移动端 header 的 logo 尺寸、留白和按钮关系
- [x] Header、footer、共创页和微信引导页接入正式横向 lockup，不再各自手写 icon + title 拼接
- [x] 统一首页、活动大厅、组队大厅、搜索、详情页、个人空间和后台的基础背景与页面节奏
- [x] 首页、共创页、微信引导和 loading test 接入 `desktopWide`、`desktopBand`、`mobileFrame` 品牌背景资产
- [x] 替换残留旧风格按钮、卡片、徽章和空态，不让 v1/v2 风格混杂
- [x] 收敛全局语义色：`coral`、`paper`、`sand`、`team-bg`、`team-border` 切到 v2.1 绿 / 奶油 / 珊瑚系统
- [x] 核心卡片第一轮统一：活动 / 组局卡片外框、顶部状态条、徽章、报名按钮、复制按钮和无图 fallback
- [x] 核心控件第一轮统一：搜索按钮、分页、导航进度条、详情页 CTA、分享 / 复制浮层和日期视图控件
- [x] 共享空状态组件接入 v2.1 品牌 icon、绿色行动按钮和更轻的纸面背景
- [x] 共享空状态和错误态接入专用 `friemi-empty-state-mark.png`
- [x] 移动端底部导航完成 v2.1 品牌试点：奶油底、品牌绿选中态、珊瑚主入口
- [x] 梳理移动端底部导航、浮层、弹窗和安全区，保证新视觉不遮挡核心操作
- [x] 统一 loading、默认分享图和 PWA icon 的品牌露出
- [x] 共享错误页组件接入 v2.1 品牌错误态
- [x] 统一公告页和页面级特殊空状态的品牌露出
- [x] 建立全站品牌视觉审计清单，记录哪些页面已完成、哪些页面仍是旧样式

验收标准：

- 网页端和移动端 logo 在主要入口都清楚、统一、不挤压导航
- 用户从首页进入活动、组队、详情和个人空间时不会感到风格断层
- 390px 移动端无横向滚动条，header / footer / bottom nav 不遮挡内容
- 1280px 桌面端不出现明显模板感、大空白或旧品牌残留
- 全站常见状态：加载、空态、错误、分享和登录页都有一致品牌表现

### 4. 全站法语 Accent 与法语文案精修

建议分支：

```text
feature/v2-1-french-accent-copy-polish
```

需求理解：

当前部分法语文案为了 ASCII 兼容省略了 accent，正式产品里会显得不专业。v2.1 需要单独做一轮法语 copy polish，把全站法语中的 é、è、ê、à、ù、ç、œ 等重音和拼写补齐，并顺手检查法语和英语长文案在移动端按钮、筛选器、卡片和分享信息里的换行。

小功能：

- [x] 扫描 `messages/fr.json`、页面内联法语 copy、邮件 / 分享 / toast / 错误提示里的无 accent 法语
- [x] 补齐常见词：activité、événement、créateur、communauté、réutilisable、préparation、précédent、détail、société 等
- [x] 代码级检查法语按钮、badge、筛选项和导航文案，压缩移动端排序和筛选短标签，避免窄屏默认态出现难看的省略号
- [x] 检查并优化法语分享标题、Open Graph description、微信分享兜底文案
- [x] 代码级检查英语按钮、badge、筛选项和导航文案，压缩移动端排序和筛选短标签，避免窄屏默认态出现难看的省略号
- [x] 检查并优化英语分享标题、Open Graph description、微信分享兜底文案
- [ ] 本地 Clerk 配置恢复后，补跑 390px 运行态截图检查，确认英法按钮、badge、筛选项和导航没有真实截断
- [x] 保留英文代码 key 和 URL slug，不为了 accent 修改路由、数据库 enum 或稳定标识

验收标准：

- 法语界面不再大面积出现缺少 accent 的文本
- 法语长文案在移动端主要入口不破坏布局
- 不修改稳定 URL、数据库字段、source 标识或代码 key
- 中文、英文文案不被误改

### 5. 网页端组件风格统一、移动端卡片设计与第二阶段风格化统一

建议分支：

```text
feature/v2-1-style-unification-phase-2
```

需求理解：

v2.1 第一轮品牌整改已经把 logo、基础色彩、header / footer、核心背景和部分共享组件推进到新风格。下一步需要进入更细的产品组件层：网页端要把按钮、筛选、下拉、分页、弹窗、卡片和状态组件统一成一套可复用的风格；移动端要重点重做组局卡片和活动卡片，让双列信息密度、图片比例、badge、状态、收藏、报名 / 操作入口在 390px 下更稳、更精致。这个阶段不是单页美化，而是网站第二步风格化统一。

小功能：

- [ ] 审计网页端常用组件：按钮、chip、badge、筛选栏、排序下拉、分页、弹窗、toast、空态和错误态的风格差异
- [ ] 统一网页端组件的圆角、边框、阴影、hover / active / disabled 状态和触控 / 点击反馈
- [x] 统一网页端活动大厅、组队大厅、搜索结果和个人空间里的卡片外框、信息层级和操作按钮
- [ ] 优化网页端筛选与排序组件，减少“框框堆叠”和临时感，保持信息密度但不拥挤
- [x] 重做移动端活动卡片设计：图片比例、标题行数、日期地点、活动 / 展览 badge、状态角标和收藏按钮更协调
- [x] 重做移动端组局卡片设计：报名人数、成员头像、状态、发起人、报名 / 再来一局入口在双列布局下不撑高、不出现难看的省略号
- [x] 落实移动端组局 action sheet：卡片内只保留短操作入口，点击后用底部浮层展示报名加入 / 再来一局，避免按钮撑高卡片
- [x] 消息页风格融入：消息列表、好友列表、聊天线程、气泡、输入栏和 loading 骨架接入 v2.1 品牌纸面风格，并修复移动端会话区上下导航遮挡
- [x] 明确移动端活动卡片和组局卡片的视觉差异，语义清楚但保持同一套 Friemi 品牌语言
- [x] 建立活动 / 组局卡片 v2.1 视觉素材板，放入品牌素材组，作为后续组件落地参考
- [ ] 检查中 / 英 / 法长文案在移动端卡片、badge、按钮和筛选控件里的换行与截断策略
- [x] 建立第二阶段组件风格规则，记录哪些组件已经统一、哪些仍保留旧样式

验收标准：

- 网页端主要组件读起来像同一套产品系统，不再像多个阶段拼接出来的样式
- 390px 移动端活动卡片和组局卡片双列稳定，按钮、badge、收藏和状态不互相遮挡
- 移动端组局卡片不会因为报名 / 再来一局 / 英法文案而异常增高
- 网页端 1280px 下卡片、筛选、分页和弹窗风格统一，信息密度合理
- 活动、组局、已结束、进行中、可报名、我发起、已报名等状态语义清楚
- 第二阶段风格规则在文档中可复用，后续页面不需要重新判断基础组件风格

### 6. 色盘驱动的全站颜色统一与全背景 Logo 场景化使用

建议分支：

```text
feature/v2-1-brand-palette-token-unification
```

需求理解：

`public` 已新增 `全背景logo.png` 和 `色盘.png`。后续需要以 `色盘.png` 作为全站唯一颜色来源，把当前散落在组件里的旧色值、临时色值和重复语义色收敛成品牌 token；同时评估 `全背景logo.png` 这种方形带底 logo 在 loading fallback、空状态、分享图、无图卡片和 PWA 场景中是否比透明 logo 更合适。

小功能：

- [x] 从 `apps/web/public/色盘.png` 提取正式品牌色值，定义主色、辅助色、文字色、背景色、边框色、状态色和阴影色 token
- [x] 盘点全站 hardcoded 色值，区分必须替换、暂时保留和第三方 / 语义例外
- [x] 将按钮、卡片、badge、筛选器、下拉栏、分页、弹窗、toast、空态和错误态切到统一色盘
- [x] 统一移动端底部导航、顶部 header、浮层、action sheet 和安全区背景色，避免新旧色混用
- [x] 评估 `apps/web/public/全背景logo.png` 的适用场景：方形图标、无图 fallback、空状态、loading reduced-motion、分享图或 PWA 入口
- [x] 保留透明 logo 和横向 lockup 的使用边界，避免所有场景都强行使用同一张图
- [x] 更新品牌视觉文档，记录色盘 token、使用规则和禁止继续新增散落色值的约定

验收标准：

- 主要页面不再出现明显的新旧颜色混搭
- 全站新增颜色优先来自 `色盘.png` 派生 token，不继续散落硬编码
- 方形全背景 logo 只出现在更合适的场景，不挤压 header 或替代横向品牌露出
- 移动端 390px 和桌面端 1280px 下按钮、卡片、下拉、弹窗颜色一致且可读

### 7. 组局详情页报名 CTA 优先级重构

建议分支：

```text
feature/v2-1-team-detail-cta-redesign
```

需求理解：

当前组局详情页的报名入口在网页端和移动端都不够明显，用户进入详情后容易先浏览信息但找不到核心操作。这个任务需要把组局详情的“报名 / 联系 / 登录 / 游客报名”操作单独重构，并且桌面端和移动端分开设计：网页端提高报名按钮的视觉优先级和固定可见性；移动端使用更轻、更灵动的悬浮圆形操作入口，点击后再展开操作浮窗，避免详情页第一屏被大按钮压住。

小功能：

- [x] 桌面端组局详情页重新安排右侧或首屏 CTA 区，报名按钮成为最高优先级操作
- [x] 桌面端报名、联系发起人、复制 / 分享、管理等操作层级清楚，避免多个按钮同权重堆叠
- [x] 移动端新增悬浮圆形 CTA 图标按钮，避开底部导航和安全区，不遮挡标题、时间、地点和报名状态
- [x] 移动端点击悬浮按钮后弹出带轻量动效的操作浮窗 / action sheet
- [x] 已登录用户操作：报名加入、联系发起人；根据状态展示已报名、待审核、满员、已结束等降级态
- [x] 未登录用户操作：登录报名、游客报名、联系发起人，并明确游客报名和登录报名的视觉层级
- [x] 浮窗顶部显示当前组局标题或短标题，避免用户误操作到其他组局
- [x] 中 / 英 / 法长文案在移动端浮窗按钮里不出现难看的省略号或文字切割
- [ ] 组局发起人 / 管理人视角不展示普通报名 CTA，改为管理、查看报名、联系参与者等操作

验收标准：

- 用户进入组局详情页后能立刻识别报名是主操作
- 桌面端 1280px 下报名按钮足够突出，但不破坏详情页信息阅读
- 移动端 390px 下悬浮 CTA 不遮挡底部导航、内容文本或弹窗按钮
- 已登录、未登录、游客报名、已报名、满员、已结束、自己发起等状态都有清楚操作
- 操作浮窗动效轻量，不拖慢页面，也不会让用户误以为页面跳转失败

### 8. 微信内登录跳转页优化

建议分支：

```text
feature/v2-1-wechat-open-browser-bridge-polish
```

需求理解：

微信内置浏览器无法稳定完成 Google 登录时，当前引导页需要更像产品内的过渡体验，而不是一张临时说明图。用户应该能快速理解“为什么要跳到浏览器”“点哪里打开浏览器”“复制链接如何兜底”，同时页面不能被背景装饰、微信顶部栏或底部安全区遮挡。

小功能：

- [x] 优化微信内登录 / 注册引导页视觉层级，品牌露出清楚但不挤压核心说明
- [x] 右上角箭头和“点这里”提示更贴近微信菜单位置，避免遮挡标题或正文
- [x] 步骤说明从大框框改成更轻的操作清单，减少说明书感
- [x] 背景品牌装饰不穿过正文和按钮，移动端 390px 下内容可读
- [x] 复制链接兜底按钮保持最高可点击性，复制成功态明确
- [x] 中 / 英 / 法文案保持短句，不出现按钮或步骤文案截断
- [x] 记录微信 WebView 引导页打开 analytics 不被破坏

验收标准：

- 微信内打开登录 / 注册页时，用户能一眼看到“用浏览器继续登录”
- 右上角菜单指引清楚，不遮挡页面内容
- 390px 移动端无横向滚动条，按钮和底部安全区不被遮挡
- 复制链接兜底可用，复制成功状态清楚

### 9. 线下活动签到功能

建议分支：

```text
feature/v2-1-offline-activity-check-in
```

需求理解：

Friemi 后续需要支持真实线下活动的到场确认。签到功能不只是“打个勾”，还需要区分报名、审核通过、实际到场、未到场等状态，帮助发起人管理现场，也为后续用户信用、活动复盘、主理人数据和常客沉淀做基础。

小功能：

- [ ] 设计签到数据模型：报名记录和签到记录的关系、签到时间、签到方式、操作人、备注和撤销状态
- [ ] 发起人 / 管理人可以在组局详情或管理页查看报名名单并执行签到
- [ ] 支持移动端现场快速签到，优先保证 390px 下单手操作可用
- [ ] 评估二维码签到方案：活动专属二维码、用户报名凭证二维码、扫码后确认到场
- [ ] 防止重复签到，已经签到的参与者显示明确状态和签到时间
- [ ] 支持手动修正：误签、取消签到、迟到备注等管理操作
- [ ] 未报名用户、待审核用户、被拒绝用户、已取消报名用户不能被误记为正常签到
- [ ] 签到状态在活动结束后仍可查看，用于发起人复盘和用户活动记录沉淀

验收标准：

- 发起人 / 管理人能在手机上完成现场签到核心流程
- 签到状态和报名状态不会混淆，待审核 / 已拒绝 / 已取消用户不能误签到
- 重复签到有明确防护和提示
- 签到数据可追溯：谁在什么时候为谁签到
- 移动端不被底部导航、弹窗或安全区遮挡

### 10. 用户账户多字段绑定与唯一性校验

建议分支：

```text
feature/v2-1-account-identity-bindings
```

需求理解：

用户账户需要像微信等产品一样支持更多身份绑定字段，例如微信号、邮箱、手机号等，方便报名联系、账号找回、好友识别和后续账号合并。但这些字段必须做唯一性约束：一个微信号 / 邮箱 / 电话只能绑定一个账户，避免同一身份被多个账号占用。

小功能：

- [x] 盘点当前用户资料字段：`email`、`wechatId`、`normalizedWechatId`、手机号、用户名、Clerk external id 等
- [x] 设计绑定字段规则：哪些字段公开展示，哪些只用于联系 / 匹配 / 找回
- [x] 微信号绑定做标准化和查重：大小写、空格、特殊字符处理一致
- [x] 邮箱绑定做标准化和查重：大小写、空格、验证状态和 Clerk 邮箱关系明确
- [x] 手机号绑定做国家区号、格式标准化和查重
- [x] 一个微信号 / 邮箱 / 电话只能绑定一个账户，重复绑定时给出清楚提示
- [x] 修改绑定字段需要校验当前用户身份，避免用户误绑或恶意占用
- [x] 解除绑定、重新绑定、账号迁移和幽灵账户合并场景有明确规则
- [x] 个人主页只展示适合公开的信息，隐私字段不直接暴露
- [x] 补充生产数据库发布说明：分类 enum 迁移、账号绑定字段结构迁移和幽灵邮箱占用清理

验收标准：

- 用户能清楚知道自己绑定了哪些身份字段
- 重复微信号 / 邮箱 / 电话不能绑定到多个账户
- 错误提示不暴露他人完整隐私信息
- 管理员或后续迁移脚本有明确排查路径
- 不破坏现有 Clerk 登录和历史用户资料

### 11. 报名需审核状态可见性优化

建议分支：

```text
feature/v2-1-participation-review-visibility
```

需求理解：

当前用户在列表、详情或报名前不够直观地看到某个组局是否需要发起人审核。需要把 `requiresApproval` 变成清楚的产品状态，而不是隐藏在报名流程里。用户应该在点击报名前就知道：报名后是直接加入，还是提交申请等待审核。

小功能：

<!-- - [ ] 组局卡片增加“需审核”tag，与活动类型、状态、已报名等 badge 保持同一套视觉语言 -->

- [x] 组局详情首屏展示是否需要审核，靠近报名 CTA 和人数信息
- [x] 报名按钮文案根据审核模式区分：直接报名 / 申请加入 / 等待审核
- [x] 报名成功反馈区分：已加入 / 申请已提交，等待发起人审核
- [x] 待审核参与者在个人空间、组局详情和消息通知中有清楚状态
- [x] 发起人视角突出待审核人数和审核入口
- [x] 中 / 英 / 法文案在移动端 badge 和按钮中不出现难看的省略号

验收标准：

- 用户报名前能一眼看出该组局是否需要审核
- 申请加入和直接加入的按钮、提示、状态不混淆
- 发起人能快速发现待审核报名
- 390px 移动端详情操作浮层中的审核提示不遮挡报名、联系等核心操作

### 12. mobile-home 九宫格分类补全与空结果体验

建议分支：

```text
feature/v2-1-mobile-home-category-fallbacks
```

需求理解：

`/mobile-home` 九宫格已经成为活动 / 组局的全局分类入口，但部分分类可能天然较冷门。用户点击后如果只看到几个内容，甚至完全没有结果，会误以为页面坏了或平台内容太少。需要为分类入口设计补全机制：优先展示对应分类，但在结果不足时自然补充相近内容、热门内容或推荐入口，让体验不空、不尴尬。

小功能：

- [x] 为九宫格分类定义结果不足阈值，例如少于 4 条时触发补全
- [x] 分类结果为空时不只显示空状态，增加“相近推荐 / 最近热门 / 全部活动”内容
- [x] 设计分类相邻关系：闲逛可补旅行 / 艺术，视听可补音乐 / 艺术，进步可补闲逛 / 其它等
- [x] 补全内容需要有清楚但克制的标记，避免用户误以为全部都是当前分类
- [x] URL query 保持当前分类上下文，返回时不丢失来源
- [x] 移动端分类页首屏保持内容密度，不因为空状态占满一屏
- [x] 统计分类点击、空结果、补全点击，帮助后续判断冷门分类是否需要调整

验收标准：

- 点击任何九宫格分类都不会出现体验很差的空白页
- 用户能理解哪些是当前分类结果，哪些是补充推荐
- 冷门分类也能引导用户继续浏览，不直接中断探索
- 390px 移动端无横向滚动条，补全区不遮挡底部导航

### 13. mobile-home 页面精进优化

建议分支：

```text
feature/v2-1-mobile-home-polish
```

需求理解：

当前 `/mobile-home` 已经完成移动端大厅的基础形态，但还需要继续精修首屏密度、快捷入口、底部导航选中态以及平板 / 网页端的真实页面布局。页面不必强制所有机型都一屏完整展示，但要在一屏内尽可能给用户看到主推荐、搜索 / 发起入口、九宫格分类和当前导航状态，避免部分机型出现内容被底部导航遮挡、“大厅”选中不明显，或平板 / 网页端像嵌入手机页面的问题。

小功能：

- [x] 将搜索栏旁边两个圆形快捷按钮合并为一个“我要组局”入口，减少首屏横向占用和视觉噪音
- [x] 合并后的“我要组局”入口要保留清楚的发起语义，图标、颜色和触控面积不低于 44px
- [x] 优化 `/mobile-home` 首屏纵向密度：主推荐卡、搜索 / 发起入口、九宫格之间的间距更紧凑但不拥挤
- [x] 解决部分机型底部导航遮挡九宫格或文字的问题，允许页面自然滚动，不强制所有内容必须塞进一屏
- [x] 在一屏前提下尽可能露出更多九宫格内容，避免用户误以为页面只有上半部分
- [x] 强化底部导航处于“大厅”时的选中态：保留当前风格，但让高亮、阴影、标记或文字状态更容易被用户识别
- [x] 中 / 英 / 法环境下检查“大厅”和其它底部导航标签不截断、不错位
- [x] 平板和网页端不再显示占位介绍页，改为真实大厅布局：左侧标题、轻量说明、搜索 / 我要组局、九宫格分类，右侧展示发现 swiper
- [x] 平板和网页端避免把移动端页面直接嵌入窄容器，内容使用两栏布局和自适应 swiper 尺寸
- [x] 优化平板端留白：主体内容上移，标题、搜索、分类和推荐卡之间形成更完整的页面节奏
- [x] 平板端底部剩余空间增加轻量内容带，补足页面节奏；手机和桌面端保持原布局不受影响
- [x] 针对 iPad mini / Air / Pro、Zenbook Fold 等高竖屏平板单独增加跨两栏底部内容带，并替换为首页同款“Live from Friemi / 从一次小局开始”活动轮播组件；手机、横屏平板和桌面不跟随变化
- [x] 网页端和平板端顶部导航顺序调整为“大厅 / 组局 / 活动 / 消息 / 我要组局”，并压缩平板导航避免遮挡 logo
- [x] `/mobile-home` 中文说明文案改为更轻巧、给用户看的语气，避免机械产品说明感

### 14. 共创主理人身份展示

建议切换分支：

```text
feature/v2-cohost-profile-identity
```

需求理解：

平台需要有“共创主理人”身份标识，并在个人主页明确展示出来。这个身份后续会承载早期认证、优先展示、产品共创和合作机会，不能只作为后台字段存在。

小功能：

- [ ] 明确共创主理人身份字段来源：Profile 字段、角色表、配置名单或后台认证状态
- [ ] 在自己的个人主页和他人个人主页展示“共创主理人”身份徽章
- [ ] 徽章样式与普通状态 badge 区分，但不过度抢占头像和昵称区域
- [ ] 移动端个人主页头部不因身份展示变高过多
- [ ] 非共创主理人不展示空占位
- [ ] 后续可扩展到活动详情页、组局卡片或主理人介绍页露出

验收标准：

- [ ] 共创主理人访问个人主页时能看到身份展示
- [ ] 他人访问共创主理人主页时也能识别该身份
- [ ] 普通用户主页不受影响
- [ ] 390px 移动端身份徽章不遮挡头像、昵称、关注和统计入口

### 15. 组局管理人

建议切换分支：

```text
feature/v2-team-co-managers
```

需求理解：

组局发起人需要能指定最多 3 位管理人协助运营组局。管理人必须是发起人的好友；被指定后可以处理邀请 / 报名审核、修改组局信息、取消组局等管理动作，降低长期组局或大型组局的组织压力。

小功能：

- [ ] 设计组局管理人数据模型：活动 / 组局与管理人 Profile 的关联关系
- [ ] 限制每个组局最多 3 位管理人
- [ ] 只能从发起人的好友中选择管理人
- [ ] 发起人可以新增、移除管理人，并看到当前管理人列表
- [ ] 管理人可以处理报名 / 邀请审核
- [ ] 管理人可以修改组局关键信息，但敏感操作需要权限边界清楚
- [ ] 管理人可以取消组局，取消动作需要二次确认和日志记录
- [ ] 详情页、管理面板和通知中区分发起人与管理人身份
- [ ] 权限校验必须在服务端 action / query 层完成，不能只靠前端隐藏按钮

验收标准：

- [ ] 发起人只能选择自己的好友作为管理人
- [ ] 第 4 位管理人无法添加，并给出清楚提示
- [ ] 管理人能进入对应组局管理入口并完成审核、修改和取消等授权动作
- [ ] 非管理人不能通过直接请求调用管理操作
- [ ] 发起人移除管理人后，对方立即失去管理权限

验收标准：

- `/mobile-home` 390px 下不会出现九宫格被底部导航硬遮挡导致无法阅读的问题
- 用户一眼能看出当前处于底部导航的“大厅”
- “我要组局”入口比两个独立圆按钮更清楚、更省空间
- 小屏设备可自然滚动，大屏移动设备首屏信息更充实
- 平板和网页端打开 `/mobile-home` 时是完整页面体验，不像嵌入手机页，也不再出现大面积空白占位
- iPad mini / Air / Pro、Zenbook Fold 等高竖屏平板不会出现下半屏大面积空白，其它不空的尺寸保持原适配
- 平板顶部导航不遮挡 logo 标题，入口顺序清楚
- 页面整体仍保持 v2.1 奶油纸面、品牌绿和轻插画风格

## 发布验收总清单

- [ ] 首页视觉完成高星级酒店式质感重构
- [x] 移动端新版首页和底部导航布局调整完成
- [x] mobile-home 平板 / 网页端真实大厅布局完成，不再显示占位页或手机页嵌入感
- [ ] 首页移动端和桌面端首屏都完成手动验收
- [ ] 核心页面移动端 / 网页端样式分离方案有明确记录
- [x] 至少完成一个核心页面的移动端 / 网页端布局拆分试点
- [x] 全站品牌风格与 logo 大整改完成一轮页面审计
- [ ] 网页端组件风格统一、移动端活动 / 组局卡片设计完成第二阶段风格化整改
- [x] 色盘驱动的全站颜色统一方案完成并接入核心组件
- [x] 组局详情页报名 CTA 在桌面端和移动端完成分层重构
- [x] 微信内登录跳转页完成移动端引导优化
- [ ] 线下活动签到功能完成数据模型和核心现场签到流程
- [x] 用户账户微信号 / 邮箱 / 电话绑定完成唯一性校验
- [ ] 需审核组局在卡片、详情和报名流程中有清楚 tag 与状态反馈
- [x] mobile-home 九宫格冷门分类有空结果补全和推荐机制
- [x] mobile-home 搜索 / 发起入口、首屏密度和“大厅”底部导航选中态完成精修
- [x] 法语 accent 与法语文案完成一轮全站修正
- [ ] 移动端 390px 没有横向滚动条
- [ ] 桌面端 1280px 没有明显大空白或布局坍塌
- [ ] 主要按钮触控面积不低于 44px
- [ ] `npm run lint` 通过
- [x] `npm run typecheck` 通过
